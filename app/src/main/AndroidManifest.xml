<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_mentxuapp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_mentxuapp"
        android:supportsRtl="true"
        android:theme="@style/Theme.MentxuApp"
        tools:targetApi="31">

        <!-- Configuración de APIs Externas -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDo0Tm_jeeUarZlxx-DVXJ3AFPsLrjJrMc" />

        <!-- Actividad Principal (Punto de entrada) -->
        <activity
            android:name=".Presentacion"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Módulo de Mapa -->
        <activity
            android:name=".Mapa.MapaActivity"
            android:exported="false" />

        <!-- Módulo Parada 1 -->
        <activity
            android:name=".Parada1.MenuAudio"
            android:exported="false" />
        <activity
            android:name=".Parada1.SopaDeLetrasActivity"
            android:exported="false" />
        <activity
            android:name=".Parada1.Huevo_Activity"
            android:exported="false" />

        <!-- Módulo Parada 2 -->
        <activity
            android:name=".Parada2.DiferenciasActivity"
            android:exported="false" />

        <!-- Módulo Parada 3 -->
        <activity
            android:name=".Parada3.MenuAudio3"
            android:exported="false" />
        <activity
            android:name=".Parada3.Relacionar"
            android:exported="false" />

        <!-- Módulo Parada 5 -->
        <activity
            android:name=".Parada5.FishingProcessActivity"
            android:exported="false"
            android:label="Arrantzaren prozesua" />

        <!-- Módulo Parada 6 -->
        <activity
            android:name=".Parada6.Parada6Activity"
            android:exported="false"
            android:label="Parada 6: Puzlea eta Test-a" />

    </application>

</manifest>
